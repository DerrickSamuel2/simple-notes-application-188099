This app is configured to run on port 3000 for both development and production:

- npm run dev -> Next dev server on :3000
- npm start   -> Next production server on :3000

If your platform injects a different port, set PORT in the environment or adjust the command line -p flag in package.json.
